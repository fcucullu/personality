# personality.franciscocucullu.com

Personal personality profile (Crystal Knows export), redesigned to match the
franciscocucullu.com design system. Static single-page site served via GitHub Pages.

- `index.html` — the page (self-contained, noindex)
- `CNAME` — custom domain for GitHub Pages
- `robots.txt` — disallow all crawlers (link-only)
